"use client";
import { FormEvent,useState } from "react";
export function LoginForm(){const [register,setRegister]=useState(false),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const payload=Object.fromEntries(new FormData(e.currentTarget)),r=await fetch(register?"/api/auth/register":"/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)}),data=await r.json();if(r.ok){const returnTo=new URLSearchParams(location.search).get("returnTo");location.href=returnTo?.startsWith("/")?returnTo:"/"}else setError(data.error);setBusy(false)}return <div className="social-compose auth-card"><h2>{register?"إنشاء حساب":"تسجيل الدخول"}</h2><form onSubmit={submit}>{register&&<input name="fullName" required minLength={2} placeholder="الاسم الكامل"/>}<input name="email" required type="email" autoComplete="email" placeholder="البريد الإلكتروني"/><input name="password" required minLength={10} type="password" autoComplete={register?"new-password":"current-password"} placeholder="كلمة المرور"/><button className="primary" disabled={busy}>{busy?"انتظر...":register?"إنشاء الحساب":"دخول"}</button></form>{error&&<p className="form-message error">{error}</p>}<button className="text-button" onClick={()=>{setRegister(x=>!x);setError("")}}>{register?"لديك حساب؟ سجّل الدخول":"ليس لديك حساب؟ أنشئ حسابًا"}</button></div>}
