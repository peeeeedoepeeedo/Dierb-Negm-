"use client";
import { FormEvent, useState } from "react";
export function StoreSetup(){
 const [busy,setBusy]=useState(false),[message,setMessage]=useState<{ok:boolean;text:string}|null>(null);
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage(null);const form=new FormData(e.currentTarget);const payload=Object.fromEntries(form.entries());
  try{const res=await fetch("/api/stores",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const data=await res.json() as {error?:string};if(!res.ok)throw new Error(data.error||"تعذر الحفظ");setMessage({ok:true,text:"تم حفظ المتجر وإرساله للمراجعة الحقيقية."});e.currentTarget.reset()}catch(err){setMessage({ok:false,text:err instanceof Error?err.message:"حدث خطأ"})}finally{setBusy(false)}}
 return <section className="form-card"><div><span className="kicker">الخطوة الأولى</span><h2>بيانات المتجر</h2><p>لن يظهر نشاطك للعامة إلا بعد مراجعة الإدارة.</p></div><form onSubmit={submit}><label>اسم النشاط<input name="name" minLength={3} required placeholder="مثال: ماركت البلد"/></label><label>رقم الهاتف<input name="phone" inputMode="tel" minLength={8} required placeholder="01xxxxxxxxx"/></label><label>واتساب<input name="whatsapp" inputMode="tel" placeholder="اختياري"/></label><label className="full">العنوان بالتفصيل<input name="address" minLength={5} required placeholder="ديرب نجم، القرية، الشارع"/></label><label className="full">وصف النشاط<textarea name="description" rows={4} placeholder="اكتب نبذة واضحة عن نشاطك"/></label><button className="primary full" disabled={busy}>{busy?"جارٍ الحفظ...":"حفظ وإرسال للمراجعة"}</button>{message&&<p className={message.ok?"form-message ok":"form-message error"}>{message.text}</p>}</form></section>
}
