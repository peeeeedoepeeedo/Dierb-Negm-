"use client";
import { FormEvent, useMemo, useState } from "react";
type Item={itemId:string;cartId:string;storeId:string;storeName:string;productId:string;name:string;price:number;stock:number;quantity:number};type Address={id:string;label:string;street:string;[key:string]:unknown};
export function CartCheckout({initialItems,initialAddresses}:{initialItems:Item[];initialAddresses:Address[]}){const [items,setItems]=useState(initialItems),[addresses,setAddresses]=useState(initialAddresses),[selected,setSelected]=useState(initialAddresses[0]?.id??""),[coupons,setCoupons]=useState<Record<string,string>>({}),[busy,setBusy]=useState(""),[message,setMessage]=useState("");const groups=useMemo(()=>Object.values(items.reduce<Record<string,Item[]>>((a,i)=>{(a[i.storeId]??=[]).push(i);return a},{})),[items]);async function remove(id:string){const res=await fetch(`/api/cart?itemId=${encodeURIComponent(id)}`,{method:"DELETE"});if(res.ok)setItems(x=>x.filter(i=>i.itemId!==id))}async function addAddress(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy("address");const form=e.currentTarget,res=await fetch("/api/addresses",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form)))}),data=await res.json() as {address?:Address;error?:string};if(res.ok&&data.address){setAddresses(x=>[...x,data.address!]);setSelected(data.address.id);form.reset()}else setMessage(data.error||"تعذر حفظ العنوان");setBusy("")}async function checkout(group:Item[]){if(!selected){setMessage("أضف عنوان التوصيل أولًا");return}setBusy(group[0].storeId);const res=await fetch("/api/orders",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({storeId:group[0].storeId,addressId:selected,couponCode:coupons[group[0].storeId]?.trim()||undefined,items:group.map(i=>({productId:i.productId,quantity:i.quantity}))})}),data=await res.json() as {order?:{orderNumber:string};error?:string};if(res.ok&&data.order){setMessage(`تم إنشاء الطلب ${data.order.orderNumber} بنجاح.`);setItems(x=>x.filter(i=>i.storeId!==group[0].storeId))}else setMessage(data.error||"تعذر إنشاء الطلب");setBusy("")}return <section className="checkout-layout"><div><span className="kicker">سلتك</span><h1>راجع طلبك</h1>{message&&<p className="form-message ok">{message}</p>}{groups.length===0?<div className="empty"><span>🛒</span><h3>السلة فارغة</h3><a className="primary" href="/market">تصفّح المنتجات</a></div>:groups.map(group=><article className="cart-group" key={group[0].storeId}><h2>{group[0].storeName}</h2>{group.map(i=><div className="cart-line" key={i.itemId}><div><b>{i.name}</b><small>{i.quantity} × {i.price.toLocaleString("ar-EG")} ج</small></div><strong>{(i.quantity*i.price).toLocaleString("ar-EG")} ج</strong><button onClick={()=>remove(i.itemId)}>حذف</button></div>)}<div className="cart-total"><span>الإجمالي قبل الخصم</span><b>{group.reduce((n,i)=>n+i.price*i.quantity,0).toLocaleString("ar-EG")} جنيه</b></div><input className="coupon-input" value={coupons[group[0].storeId]??""} onChange={e=>setCoupons(x=>({...x,[group[0].storeId]:e.target.value.toUpperCase()}))} placeholder="كود الخصم (اختياري)"/><button className="primary" disabled={busy===group[0].storeId} onClick={()=>checkout(group)}>{busy===group[0].storeId?"جارٍ إنشاء الطلب...":"تأكيد الطلب والدفع عند الاستلام"}</button></article>)}</div><aside className="address-card"><h2>عنوان التوصيل</h2>{addresses.map(a=><label key={a.id}><input type="radio" name="address" checked={selected===a.id} onChange={()=>setSelected(a.id)}/><span><b>{a.label}</b><small>{a.street}</small></span></label>)}<form onSubmit={addAddress}><h3>عنوان جديد</h3><input name="label" required minLength={2} placeholder="المنزل أو العمل"/><input name="street" required minLength={5} placeholder="القرية، الشارع، علامة مميزة"/><input name="building" placeholder="رقم المبنى"/><button disabled={busy==="address"}>حفظ العنوان</button></form></aside></section>}
