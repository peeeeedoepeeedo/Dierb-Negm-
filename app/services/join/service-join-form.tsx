"use client";
import { FormEvent,useState } from "react";
export function ServiceJoinForm(){const [busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const form=e.currentTarget,res=await fetch("/api/services",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form)))}),data=await res.json() as {error?:string};if(res.ok){setMessage("تم حفظ ملفك وإرساله للإدارة للمراجعة.");form.reset()}else setMessage(data.error||"تعذر الحفظ");setBusy(false)}return <section className="form-card"><form onSubmit={submit}><label>الاسم<input name="name" required minLength={3}/></label><label>المهنة أو الخدمة<input name="profession" required minLength={2}/></label><label>الهاتف<input name="phone" required minLength={8}/></label><label>واتساب<input name="whatsapp"/></label><label>السعر يبدأ من<input name="priceFrom" type="number" min="0"/></label><label className="full">نبذة عن خبرتك<textarea name="description" required minLength={10} rows={5}/></label><button className="primary full" disabled={busy}>{busy?"جارٍ الحفظ...":"إرسال للمراجعة"}</button>{message&&<p className="form-message ok">{message}</p>}</form></section>}
